# HDX configuration
hdx_prod_site:
  url: "https://data.humdata.org/"
  username: ~
  password: ~
hdx_test_site:
  url: "https://test-data.humdata.org/"
  username: 'lala'
  password: 'lalala'
dataset:
  required_fields:
    - name
    - title
    - dataset_date
resource:
  dataset_id: package_id
  required_fields:
    - package_id
    - name
    - description
galleryitem:
  dataset_id: dataset_id
  required_fields:
    - dataset_id
    - title
  ignore_on_update:
    - dataset_id